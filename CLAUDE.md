# CLAUDE Notes
- Read and follow `AGENTS.md` before making edits.
- Keep `index.html` and `training.html` navigable from the shared top header links.
- Keep global visual changes (like background tone) centralized in `styles.css`.
- Preserve long-form training page narrative content when making layout or navigation updates.
